# Database Configuration
# For local development (with docker-compose)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/energy?schema=public"

# For Docker container (internal network)
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/energy?schema=public"

# Application Configuration
PORT=3000

# PostgreSQL Configuration (used by docker-compose)
POSTGRES_DB=energy
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
